# PROGRAMMING

Use this folder for general programming files you want auto-recorded in Git.

Do not place real secrets here. Use ignored `.env` files for private API keys and commit only `.env.example` files.
