# LEET CODE

Keep accepted LeetCode solutions here.

Safari does not have the same simple LeetCode-to-GitHub extension workflow as Chrome. For now, save the accepted code to a local file and run:

```bash
scripts/save_leetcode_solution.sh two-sum cpp ~/Desktop/two-sum.cpp
```

The auto-push job will also pick up future files placed inside this folder.
